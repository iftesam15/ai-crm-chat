# Google Gemini API Key (used by Next.js via .env.local)
GEMINI_API_KEY=your_gemini_api_key_here

# SQLite Database URL for Prisma CLI (requires a `.env` file — Prisma does not load `.env.local`)
DATABASE_URL="file:./dev.db"
